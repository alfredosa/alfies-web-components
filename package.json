{
  "name": "alfies-web-components",
  "private": true,
  "description": "Personal React component packages published under the @alfiexco npm scope.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "clean": "npm run clean --workspaces",
    "typecheck": "npm run typecheck --workspaces",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "react": "^18.3.1",
    "tsup": "^8.1.0",
    "typescript": "^5.5.4"
  }
}
